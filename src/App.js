import React from "react";

function App() {
  return (
    <div style={{ textAlign: "center", marginTop: "50px" }}>
      <h1>🚀 React + Docker Multi-Stage Build Demo</h1>
      <p>This app was built using a Docker multi-stage build setup!</p>
    </div>
  );
}

export default App;
